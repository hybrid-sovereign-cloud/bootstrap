---
- name: Apply AAP Controller configuration via infra.aap_configuration dispatch
  ansible.builtin.import_role:
    name: infra.aap_configuration.dispatch
  vars:
    controller_configuration_dispatcher_roles:
      - organizations
      - credentials
      - execution_environments
      - projects
      - inventories
      - hosts
      - job_templates
