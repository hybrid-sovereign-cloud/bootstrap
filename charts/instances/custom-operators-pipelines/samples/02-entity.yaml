# Entity (cluster-scoped) — creates entity-acme namespace
apiVersion: hybridsovereign.redhat/v1alpha1
kind: Entity
metadata:
  name: acme
spec:
  residency: iaas
  adminGroup:
    - acme/admins
  readGroup:
    - acme/readers
  developerGroup:
    - acme/developers
