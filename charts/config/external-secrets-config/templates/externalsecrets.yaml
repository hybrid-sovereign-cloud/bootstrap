{{- range $es := .Values.externalSecrets }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ $es.name }}
  namespace: {{ default $.Release.Namespace $es.namespace }}
  labels:
    {{- include "chart.labels" $ | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    {{- if $es.namespace }}
    name: {{ $.Values.secretStoreName }}
    kind: ClusterSecretStore
    {{- else }}
    name: {{ $.Values.secretStoreName }}
    kind: SecretStore
    {{- end }}
  target:
    name: {{ $es.name }}
    creationPolicy: Owner
  data:
    {{- range $es.keys }}
    - secretKey: {{ .k8sKey }}
      remoteRef:
        key: {{ $es.remoteKey }}
        property: {{ .vaultKey }}
    {{- end }}
{{- end }}
