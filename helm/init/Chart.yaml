apiVersion: v2
name: sovereign-init
description: >
  Bootstrap entry-point chart. Creates the ArgoCD ApplicationSet that drives
  app-of-apps on the central cluster, registers the services cluster with
  ArgoCD, and creates the required Git repository secret.
type: application
version: 0.1.1
appVersion: "1.0.0"
keywords:
  - argocd
  - bootstrap
  - gitops
  - applicationset
home: https://github.com/hybrid-sovereign/bootstrap
maintainers:
  - name: platform-team
